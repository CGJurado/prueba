import { UsersOnlyDirective } from './users-only.directive';

describe('UsersOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new UsersOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
